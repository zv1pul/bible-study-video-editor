# Copy to .env, or better: put these in .streamlit/secrets.toml which the app
# reads automatically. Both tiers are free. Never commit either file.
#
# Google AI Studio -> https://aistudio.google.com/apikey
GEMINI_API_KEY=
# Groq (hosted Whisper + Llama) -> https://console.groq.com/keys
GROQ_API_KEY=
