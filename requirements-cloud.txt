# Slimmer install for free web hosts with little RAM (e.g. Streamlit Community
# Cloud). Leaves out faster-whisper, so transcription runs through the free
# hosted Groq Whisper API instead of on the server. The app detects that
# faster-whisper is missing and hides the local option.
streamlit>=1.50,<2
moviepy>=2.1.2,<3
imageio-ffmpeg>=0.5.1,<1
Pillow>=10.1,<13
numpy>=1.26,<3
pandas>=2.0,<4
requests>=2.32,<3
proglog>=0.1.10,<1
